{
  "description": "Optional post-write path check. Never applies patches. Does not interpolate unsanitized file paths into a shell.",
  "hooks": {
    "PostToolUse": [
      {
        "tool": "write_file",
        "command": "python scripts/hook_validate_write.py"
      }
    ]
  }
}
